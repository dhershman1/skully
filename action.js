const attack = require('action.attack')
const build = require('action.build')
const heal = require('action.heal')
const move = require('action.move')
const repair = require('action.repair')
const transfer = require('action.transfer')
const upgrade = require('action.upgrade')

module.exports = {
  attack,
  build,
  heal,
  move,
  repair,
  transfer,
  upgrade
}
